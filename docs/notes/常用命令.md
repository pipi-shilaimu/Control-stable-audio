# ======================
# 数据下载（增量，每次 --count N，上传服务器后删本地再跑下一批）
# ======================

# 下载 N 个 tar（每个 ~5.4GB，默认 cdn.freesound.org 镜像）
python stable_audio_control/data/download_mtg.py --count 5

# 如果镜像慢，换 Google Drive
python stable_audio_control/data/download_mtg.py --count 5 --from gdrive

# 查看下载进度
python stable_audio_control/data/download_mtg.py --status

# 重置进度（从头开始）
python stable_audio_control/data/download_mtg.py --reset

# 从指定 tar 开始（数字或文件名，覆盖状态文件）
python stable_audio_control/data/download_mtg.py --count 5 --start-from 10
python stable_audio_control/data/download_mtg.py --count 5 --start-from raw_30s_audio-15.tar

# 手动筛出 no_vocals（从 train.json 中找出 tags 不含 ---voice 的条目）
python3 stable_audio_control/data/filter_no_vocals.py

# ======================
# 数据准备
# ======================

# 切分音频为 10s 片段（ffmpeg stream copy，不重编码）
python3 stable_audio_control/data/split_10s.py \
    --audio-dir stable_audio_control/data/mtg_jamendo_full/audio/no_vocals

# 不传 --manifest 就不生成 manifest
# 切完输出到 no_vocals_10s/，每段 10s，码率/格式与原文件一致


# ======================
# organize_dataset：一键组织数据集
# ======================

# 给定音频目录和 prompt manifest JSON，自动：
#   1. 复制 manifest → manifests/{分类名}.json
#   2. 生成 dataset_config_train.json 到音频目录的上级
#
# 目录结构要求：
#   dataset_root/
#     audio/
#       my_category/          ← --audio-dir 指向这里
#         file1.wav
#         file2.wav
#     manifests/              ← 自动创建，manifest 复制到这里

# 使用方法
python3 stable_audio_control/data/organize_dataset.py \
    --audio-dir /path/to/dataset_root/audio/my_category \
    --manifest /path/to/prompt_manifest.json

# 生成的文件：
#   dataset_root/manifests/my_category.json           ← manifest 副本
#   dataset_root/audio/dataset_config_train.json       ← 训练用的 dataset config

# 然后用生成的 config 启动训练
python3 stable_audio_control/scripts/train_controlnet_dit.py \
    --dataset-config /path/to/dataset_root/audio/dataset_config_train.json \
    --seconds-total 10 ...


# 删除切分后时长不足的碎片片段（如末尾不足 10s 的尾巴）
# --dry-run 先预览，确认后再去掉 --dry-run 执行
python3 stable_audio_control/data/delete_short_audio.py --dir no_vocals_10s --min-duration 9 --dry-run
python3 stable_audio_control/data/delete_short_audio.py --dir no_vocals_10s --min-duration 9


# 验证数据完整性
python3 -c "
import json
from pathlib import Path

manifest_path = Path('\''stable_audio_control/data/mtg_jamendo_full/manifests/no_vocals_10s.json'\'')
audio_dir = Path('\''stable_audio_control/data/mtg_jamendo_full/audio/no_vocals_10s'\'')

manifest = json.loads(manifest_path.read_text('\''utf-8'\''))
manifest_keys = set(manifest.keys())
audio_files = set(f.name for f in audio_dir.glob('\''*.mp3'\'')) | set(f.name for f in audio_dir.glob('\''*.wav'\''))

only_manifest = manifest_keys - audio_files
only_audio = audio_files - manifest_keys
empty_prompts = [k for k, v in manifest.items() if not v.get('\''prompt'\'', '\'''\'').strip()]

print(f'\''manifest: {len(manifest_keys)}, audio: {len(audio_files)}'\'')
print(f'\''only in manifest: {len(only_manifest)}, only audio: {len(only_audio)}, empty prompts: {len(empty_prompts)}'\'')
if only_manifest or only_audio or empty_prompts:
    print('\''ISSUES FOUND'\'')
" 2>&1


# ======================
# 推理 / 生成对比
# ======================


# ======================
# 推理 / 生成对比
# ======================

# ---- 单次生成：指定参考音频 + prompt + seed ----
# （无 base 模型对比，纯 ControlNet 推理）
python3 stable_audio_control/scripts/compare_controlnet_generation.py \
    --ckpt-path outputs/formal10s/checkpoints/interrupted-step-24099.ckpt \
    --reference-audio audio/Then.mp3 \
    --prompt "beautiful music, piano instrumental" \
    --output-dir outputs/formal10s/samples/ \
    --model-half \
    --num-control-layers 12 \
    --melody-feature cqt \
    --cqt-backend librosa \
    --steps 100 \
    --seconds-total 10 \
    --seed 0 \
    --control-scale 1.0 \
    --cfg-scale 7

# ---- 批量生成：同参考音频，N 个不同 seed（一次加载模型）----
python3 stable_audio_control/scripts/batch_generate_control.py \
    --ckpt-path outputs/formal10s/checkpoints/interrupted-step-48000.ckpt \
    --reference-audio audios/Then.mp3 \
    --prompt "a beautiful music, piano instrumental" \
    --output-dir outputs/formal10s/samples/ \
    --num-samples 10 \
    --seed-start 0 \
    --model-half \
    --num-control-layers 12 \
    --melody-feature cqt \
    --cqt-backend librosa \
    --steps 100 \
    --seconds-total 10 \
    --control-scale 1.0 \
    --cfg-scale 5

# 输出格式：control_{i:03d}_seed-{seed}.wav
# 一次模型加载，N 次生成，不用反复加载 checkpoint

# ---- control_scale=0 vs 1.0 对比（判断 ControlNet 效果）----
# scale=0：纯 text-to-music，无旋律控制
# scale=1：text + melody，有旋律控制
# 输出 control_0.wav, control_1.wav, ...
for i in $(seq 0 9); do
    python3 stable_audio_control/scripts/compare_controlnet_generation.py \
        --ckpt-path outputs/formal10s/checkpoints/interrupted-step-24099.ckpt \
        --reference-audio audio/Then.mp3 \
        --prompt "beautiful music, piano instrumental" \
        --output-dir outputs/formal10s/samples/ \
        --model-half \
        --num-control-layers 12 \
        --melody-feature cqt \
        --cqt-backend librosa \
        --steps 100 \
        --seconds-total 10 \
        --seed $i \
        --control-scale 1.0 \
        --cfg-scale 7 \
        --control-output-name "control_${i}.wav"
done

# ---- control_scale=0 vs 1.0 对比（判断 ControlNet 效果）----
# scale=0：纯 text-to-music，无旋律控制
# scale=1：text + melody，有旋律控制
# 同 seed、同参考音频、同 prompt 才能公平对比

# 使用同一个 checkpoint，同一段参考音频提取旋律，同一 prompt 和 seed
# 两段输出的差异 = ControlNet 学到的旋律控制能力

# 无控制（纯 text-to-music）
python3 stable_audio_control/scripts/compare_controlnet_generation.py \
    --ckpt-path outputs/formal10s/lightning_logs/version_0/checkpoints/controlnet-step=16000.ckpt \
    --reference-audio stable_audio_control/data/mtg_jamendo/train_30/0095400_seg000.wav \
    --prompt "piano instrumental" \
    --output-dir outputs/controlnet_compare_16000 \
    --model-half \
    --num-control-layers 12 \
    --melody-feature cqt \
    --cqt-backend librosa \
    --steps 100 \
    --seconds-total 47 \
    --seed 0 \
    --control-scale 0.0 \
    --cfg-scale 7

# 有控制（text + melody）
python3 stable_audio_control/scripts/compare_controlnet_generation.py \
    --ckpt-path outputs/formal10s/checkpoints/interrupted-step-24099.ckpt \
    --reference-audio audio/Then.mp3 \
    --prompt "beautiful music,piano instrumental" \
    --output-dir outputs/formal10s/samples/ \
    --model-half \
    --num-control-layers 12 \
    --melody-feature cqt \
    --cqt-backend librosa \
    --steps 100 \
    --seconds-total 10 \
    --seed 0 \
    --control-scale 1.0 \
    --cfg-scale 7

# 判断标准：control_scale=1.0 的输出旋律更接近参考音频 → ControlNet 在工作
# 使用 Audacity 打开两段音频，看频谱图和耳朵听对比

# 想在第3000步存一份checkpoint可加：
#   --ckpt-at-step 3000 \
# 配合save_last=True（Ctrl+C时自动保存last.ckpt），两个机制互补

抽卡
python3 stable_audio_control/scripts/compare_controlnet_generation_random.py \
  --ckpt-path "./outputs/train_controlnet_dit_cqt_12layers_5000_bs4/lightning_logs/version_2/checkpoints/epoch=26-step=5000.ckpt" \
  --reference-root ./stable_audio_control/data/song_describer/train \
  --num-samples 1\
  --fixed-seed 285676 \
  --prompt "Warm arpeggios on an analog synthesizer with a gradually rising filter cutoff and a reverb tail." \
  --output-dir ./outputs/controlnet_generation_random \
  --model-half \
  --num-control-layers 12 \
  --steps 100 \
  --seconds-total 20 \
  --control-scale 0 \
  --cfg-scale 7

原base与control对比
python3 stable_audio_control/scripts/compare_controlnet_generation.py \
  --ckpt-path "./outputs/train_controlnet_dit_cqt_12layers_5000_bs4/lightning_logs/version_2/checkpoints/epoch=26-step=5000.ckpt" \
  --reference-audio "./stable_audio_control/data/song_describer/train/000597_track_id-48721_caption_id-1065.wav" \
  --prompt "Warm arpeggios on an analog synthesizer with a gradually rising filter cutoff and a reverb tail." \
  --output-dir ./outputs/controlnet_generation_compare_scale0 \
  --model-half \
  --num-control-layers 12 \
  --melody-feature cqt \
  --cqt-backend librosa \
  --steps 100 \
  --seconds-total 47 \
  --seed 12345 \
  --control-scale 1.0 \
  --cfg-scale 7

# 确定性encoder调试（单batch过拟合用）
# --deterministic-encode true  绕过VAE bottleneck随机采样，用encoder mean
# 正常训练默认 false，走原生 pretrainform.encode()


python3 stable_audio_control/scripts/train_controlnet_dit.py \
    --dataset-config stable_audio_control/data/overfit_then/dataset_config_train.json \
    --max-steps  1500 \
    --batch-size 1 \
    --num-control-layers 12 \
    --precision bf16-mixed \
    --learning-rate 1e-5 \
    --default-root-dir outputs/overfit_then \
    --sigint-save true \
    --deterministic-encode True \
    --ckpt-path outputs/overfit_then/checkpoints/controlnet-step-1000.ckpt \
    --ckpt-at-step 1500
新的过拟合（带demo）
  python3 stable_audio_control/scripts/train_controlnet_dit.py \
      --dataset-config stable_audio_control/data/overfit/audios/dataset_config_train.json \
      --max-steps 1500 \
      --seconds-total 10 \
      --batch-size 4 \
      --num-control-layers 8 \
      --demo-every 250 \
      --precision bf16-mixed \
      --demo-control-scales "1.0" \
      --default-root-dir outputs/overfit \
      --demo-control-audio audios/Then.mp3 \
      --demo-prompt "instrumental piano melody, clear lead melody, no vocals,a arpeggiated harp melody" \
      --demo-control-variants "correct" \
      --demo-steps 100 \
      --sigint-save false \
      --ckpt-path outputs/overfit/checkpoints/controlnet-step-750.ckpt \
      --demo-cfg-scales "6" \
      --learning-rate 5e-5 \
      --ckpt-at-step 1500

5.29训练
  python3 stable_audio_control/scripts/train_controlnet_dit.py \
    --dataset-config stable_audio_control/data/mtg_jamendo/dataset_config_train.json \
    --max-steps 200000 \
    --batch-size 2 \
    --num-control-layers 12 \
    --precision bf16-mixed \
    --learning-rate 1e-4 \
    --log-every-n-steps 10 \
    --default-root-dir outputs/formal10s \
    --sigint-save true \
    --demo-every 5000 \
    --ckpt-path outputs/formal10s/checkpoints/interrupted-step-24099.ckpt \
    > train.log 2>&1 &

6.1训练（训练音频49563）
    python3 stable_audio_control/scripts/train_controlnet_dit.py \
    --dataset-config stable_audio_control/data/mtg_jamendo1/audio/dataset_config_train.json \
    --max-steps 49560 \
    --batch-size 4 \
    --num-control-layers 10 \
    --precision bf16-mixed \
    --learning-rate 1e-5 \
    --default-root-dir outputs/formal10s_v2 \
    --sigint-save true \
    --demo-every 1000 \
    --ckpt-at-step 10800 \
    --ckpt-every-n-steps 12390\
    > train.log 2>&1 &