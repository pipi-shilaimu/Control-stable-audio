开始训练CQT
python3 stable_audio_control/scripts/train_controlnet_dit.py \
  --dataset-config stable_audio_control/data/song_describer/dataset_config_train.json \
  --max-steps 5000 \
  --batch-size 4 \
  --num-workers 4 \
  --precision bf16-mixed \
  --melody-feature cqt \
  --num-control-layers 12 \
  --learning-rate 5e-5 \
  --default-root-dir outputs/train_controlnet_dit_cqt_12layers_5000_bs4

# 想在第3000步存一份checkpoint可加：
#   --ckpt-at-step 3000 \
# 配合save_last=True（Ctrl+C时自动保存last.ckpt），两个机制互补

抽卡
python3 stable_audio_control/scripts/compare_controlnet_generation_random.py \
  --ckpt-path "./outputs/train_controlnet_dit_cqt_12layers_5000_bs4/lightning_logs/version_2/checkpoints/epoch=26-step=5000.ckpt" \
  --reference-root ./stable_audio_control/data/song_describer/train \
  --num-samples 1\
  --fixed-seed 285676 \
  --prompt "Warm arpeggios on an analog synthesizer with a gradually rising filter cutoff and a reverb tail." \
  --output-dir ./outputs/controlnet_generation_random \
  --model-half \
  --num-control-layers 12 \
  --steps 100 \
  --seconds-total 20 \
  --control-scale 0 \
  --cfg-scale 7

原base与control对比
python3 stable_audio_control/scripts/compare_controlnet_generation.py \
  --ckpt-path "./outputs/train_controlnet_dit_cqt_12layers_5000_bs4/lightning_logs/version_2/checkpoints/epoch=26-step=5000.ckpt" \
  --reference-audio "./stable_audio_control/data/song_describer/train/000597_track_id-48721_caption_id-1065.wav" \
  --prompt "Warm arpeggios on an analog synthesizer with a gradually rising filter cutoff and a reverb tail." \
  --output-dir ./outputs/controlnet_generation_compare_scale0 \
  --model-half \
  --num-control-layers 12 \
  --melody-feature cqt \
  --cqt-backend librosa \
  --steps 100 \
  --seconds-total 47 \
  --seed 12345 \
  --control-scale 1.0 \
  --cfg-scale 7


python3 stable_audio_control/scripts/train_controlnet_dit.py \
    --dataset-config stable_audio_control/data/mtg_jamendo/dataset_config_train_30.json \
    --max-steps  50000 \
    --batch-size 2 \
    --num-control-layers 12 \
    --precision bf16-mixed \
    --learning-rate 1e-4 \
    --log-every-n-steps 10 \
    --default-root-dir outputs/overfit_piano_30 \
    --ckpt-at-step 25000 \
    --sigint-save false \
    --deterministic-encode false
    > train.log 2>&1 &


# 确定性encoder调试（单batch过拟合用）
# --deterministic-encode true  绕过VAE bottleneck随机采样，用encoder mean
# 正常训练默认 false，走原生 pretrainform.encode()